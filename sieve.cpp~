#include<iostream>
#include<cmath>
using namespace std;

void sieve(int *x,int n)
{int i,j;
  for(i=2;i<=sqrt(n);i++)
    {if(x[i-2]==0)
	{for(j=i*i;j<=n;j=j+i)
	    {x[j-2]=1;}}
    }
}

main()
  {int a,m,n,*x,i,j;
    cin>>a;
    for(i=0;i<a;i++)
      {cin>>m;
	cin>>n;
	x=new int[n];
	for(j=0;j<n;j++)
	  {x[j]=0;}
	sieve(x,n);
	for(j=m;j<=n;j++)
	  {if(j==1)j++;
	    if(x[j-2]==0) cout<<j<<"\n";}
      }
  }
