#include<iostream>
#include<cmath>
using namespace std;

int isprime(int n)
{int i;
  if(n==1) return 0;
  if(n>3&&n%2==0) return 0;
  for(i=3;i<=sqrt(n);i=i+2)
    {if(n%i==0){return 0;}}
  return 1;
}

main()
{int a,i,j,x[20],y;
  cin>>a;
  for(i=0;i<a;i++)
    {cin>>x[i];
      cin>>x[i+1];
      {for(j=x[i];j<=x[i+1];j++)
	{if(isprime(j)==1)
	    {cout<<j<<"\n";}}}
      cout<<"\n";}
}
